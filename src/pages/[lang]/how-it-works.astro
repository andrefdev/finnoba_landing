---
import Base from "../../layouts/Base.astro";
import PageHero from "../../components/common/PageHero.astro";
import Section from "../../components/common/Section.astro";
import SectionHeading from "../../components/common/SectionHeading.astro";
import Card from "../../components/common/Card.astro";
import Eyebrow from "../../components/common/Eyebrow.astro";
import NumberBadge from "../../components/common/NumberBadge.astro";
import DataTable from "../../components/common/DataTable.astro";
import BulletList from "../../components/common/BulletList.astro";
import LevelLadder from "../../components/loans/LevelLadder.astro";
import PageCta from "../../components/common/PageCta.astro";
import Button from "../../components/common/Button.astro";
import { N1, usd } from "../../lib/loans";
import { dict, localeParams, localePath, type Lang } from "../../lib/i18n";

export const getStaticPaths = localeParams;

const { lang } = Astro.props as { lang: Lang };
const t = dict(lang);
const c = t.how;

// Level 1 is published in full — it's the only level the pilot originates, and
// a borrower shouldn't need a call to find out what the money costs. Higher
// levels stay off the ladder; they're agreed in writing per business.
const scheduleRows = [
  ...N1.rows.map((r, i) => [
    `${c.scheduleHead[0]} ${i + 1}`,
    usd(r.capital),
    usd(r.interest),
    usd(r.payment),
  ]),
  [
    c.scheduleTotalLabel,
    usd(N1.totals.capital),
    usd(N1.totals.interest),
    usd(N1.totals.payment),
  ],
];

const monthWord = lang === "es" ? "meses" : "months";
---

<Base lang={lang} path="/how-it-works" title={c.meta.title} description={c.meta.description}>
  <PageHero title={c.h1} subtitle={c.sub} />

  <Section topRound>
    <SectionHeading eyebrow="01" title={c.stepsTitle} />
    <div class="grid steps">
      {c.steps.map((s, i) => (
        <Card class="step">
          <NumberBadge n={i + 1} />
          <h3 class="step-title">{s.t}</h3>
          <p class="step-body">{s.b}</p>
        </Card>
      ))}
    </div>
  </Section>

  <Section paddingY="32px">
    <SectionHeading eyebrow="02" title={c.levelsTitle} lead={c.levelsIntro} />
    <LevelLadder levelWord={t.home.markets.levelLabel} monthWord={monthWord} />
    <p class="footnote">{c.levelsFoot}</p>

    <h3 class="sub-title">{c.scheduleTitle}</h3>
    <p class="lead">{c.scheduleLead}</p>
    <DataTable header={c.scheduleHead} rows={scheduleRows} class="narrow" />
    <p class="footnote">{c.scheduleFoot}</p>
    <Button href={localePath(lang, "/apply")} class="schedule-cta">{c.scheduleCta}</Button>

    <h3 class="sub-title">{c.requirementsTitle}</h3>
    <BulletList items={c.requirements} />
  </Section>

  <Section paddingY="32px">
    <SectionHeading eyebrow="03" title={c.flowTitle} />
    <ol class="grid flow">
      {c.flow.map((node, i) => (
        <li>
          <Card class="node">
            <Eyebrow tone="muted">{`0${i + 1}`}</Eyebrow>
            <p class="node-title">{node.t}</p>
            <p class="node-body">{node.b}</p>
          </Card>
        </li>
      ))}
    </ol>
  </Section>

  <Section>
    <SectionHeading eyebrow="04" title={c.tableTitle} />
    <DataTable rows={c.table} class="narrow" />
  </Section>

  <PageCta lang={lang} title={c.cta} primary={c.cta} href="/investors" />
</Base>

<style>
  .grid { display: grid; gap: 16px; }
  .steps { grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); }

  .flow {
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 12px;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .step { padding: 28px; }

  .step-title {
    font-family: var(--font);
    font-weight: 700;
    font-size: 20px;
    letter-spacing: -0.015em;
    color: var(--ink);
    margin: 18px 0 10px;
  }

  .step-body {
    font-family: var(--font);
    font-size: 14.5px;
    line-height: 1.55;
    color: var(--ink-2);
  }

  .node { padding: 20px; height: 100%; }

  .node-title {
    font-family: var(--font);
    font-weight: 700;
    font-size: 15px;
    color: var(--ink);
    margin-bottom: 6px;
  }

  .node-body {
    font-family: var(--font);
    font-size: 13px;
    line-height: 1.5;
    color: var(--muted);
  }

  .narrow { max-width: 720px; }

  .footnote {
    margin-top: 28px;
    max-width: 620px;
    font-family: var(--font);
    font-size: 13px;
    line-height: 1.6;
    color: var(--muted);
  }

  .schedule-cta { margin-top: 20px; }

  .lead {
    max-width: 620px;
    margin: -8px 0 18px;
    font-family: var(--font);
    font-size: 15px;
    line-height: 1.6;
    color: var(--ink-2);
  }

  .sub-title {
    margin: 40px 0 18px;
    font-family: var(--font);
    font-weight: 700;
    font-size: 20px;
    letter-spacing: -0.015em;
    color: var(--ink);
  }
</style>
